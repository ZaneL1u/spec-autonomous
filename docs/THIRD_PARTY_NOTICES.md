# Third-party notices

The generated `.agents/skills/openspec-*` guidance comes from OpenSpec 1.13.0.
Its MIT license is retained at [licenses/openspec-MIT.txt](licenses/openspec-MIT.txt).

Research checkouts under `.references/` retain their upstream licenses and are not
committed or included in the npm package. Reports link to pinned source commits.
The project implementation is original and licensed under the root MIT license.

`tests/fixtures/speckit-native/` contains unmodified Spec Kit command templates from
the research-locked commit, with its MIT license. They are offline test inputs and
are not included in npm runtime packages.
