# Spec Autonomous dogfood playground

This checked-in fixture is initialized as OpenSpec by `scripts/dogfood.mjs` in a temporary copy. It exercises `init`, `prepare`/`auto`, host claim and receipt handling, progress, and cleanup preview without writing the source playground.
